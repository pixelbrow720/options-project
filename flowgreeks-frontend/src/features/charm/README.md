# features/charm

Vanna / charm Greeks views (cross-Greek surfaces).

**Backend payload:** Vanna and charm ride inside the snapshot envelope
under `gex` and `zero_dte` extras. The dashboard-wide "charm
landscape" lives in `features/zero-dte/` — this folder is reserved for
the back-month vanna/charm decomposition card if/when it ships.

**Status:** placeholder. Reserve the namespace, document scope.
